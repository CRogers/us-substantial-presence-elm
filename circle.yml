machine:
  node:
    version: 6.1.0

general:
  branches:
    ignore:
      - gh-pages

dependencies:
  override:
    - npm install -g elm

test:
  override:
    - ./make.sh

deployment:
  master:
    branch: master
    owner: CRogers
    commands:
      - ./scripts/deploy-gh-pages.sh